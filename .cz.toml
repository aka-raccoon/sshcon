[tool.commitizen]
name = "cz_conventional_commits"
version = "0.3.3"
version_files = [
    "setup.cfg:version",
    "docs/conf.py: release"
]
tag_format = "$version"
bump_message = "bump: $current_version → $new_version"
